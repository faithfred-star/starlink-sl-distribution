name: Keep Site Alive
on:
  schedule:
    - cron: '*/12 * * * *'  # Runs every 12 minutes
  workflow_dispatch:        # Allows manual testing

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit the website
        run: curl -I https://starlink-sl-distribution.onrender.com/